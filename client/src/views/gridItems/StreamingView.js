import React from 'react';
import PropTypes from 'prop-types';

const StreamingView = props => {
  return (
    <div>
      StreamingView
    </div>
  );
};

StreamingView.propTypes = {
  
};

export default StreamingView;